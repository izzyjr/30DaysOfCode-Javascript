function main() {
    var S = readLine();
    var final = parseInt(S);
    
    try{
        console.log(final.toString().replace('NaN', 'Bad String'));
    }
    
    catch(error){
        
    }

    
}